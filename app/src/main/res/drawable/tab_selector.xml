<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected tab (initial dark color with transparency) -->
    <item android:state_selected="true" android:color="@color/selected_tab_color"/>

    <!-- Unselected tab (light grey color) -->
    <item android:state_selected="false" android:color="@color/light_selected_tab_color"/>
</selector>
